import { HijriCalendar } from "@/components/hijri-calendar";

export const metadata = { title: "Hijri Calendar — Daily Aamal" };

export default function CalendarPage() {
  return <HijriCalendar />;
}
